
# .htaccess
# Forwards all requests to the 'public' folder.

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Forward all requests to public/
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
